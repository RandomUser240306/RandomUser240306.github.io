var originalWord = prompt("Type a word");
var pigLatinAddon = "ay";
var wordCharacters = originalWord.length;
var newWord = originalWord(1,wordCharacters) + orignalWord(0) + pigLatinAddon;

function myFunction() {
  document.getElementById("output").innerHTML = string(newWord);
}